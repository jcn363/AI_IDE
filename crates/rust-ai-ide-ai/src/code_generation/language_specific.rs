//! # Language-Specific Generation Module

use crate::code_generation::*;

#[derive(Debug)]
pub struct LanguageSpecificGenerator;

impl Default for LanguageSpecificGenerator {
    fn default() -> Self {
        Self
    }
}
